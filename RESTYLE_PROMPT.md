# Review Mate 리스타일 지시서 — 토스/당근 스타일 미니멀

당신은 이 리포지토리의 `index.html`의 **디자인만** 교체하는 담당입니다. 이 문서가 유일한 스펙입니다.

## 절대 규칙 (기능 보호)

- **`<script>` 블록은 한 글자도 수정 금지.** PRESETS, SAFETY_BLOCKS, 조립기, 이벤트 바인딩 전부 그대로.
- 프롬프트 문구·라벨·placeholder·안내 문구 등 **모든 텍스트 내용 변경 금지.**
- HTML 구조는 유지. 단, 스타일링에 필요한 class 추가/변경과 순수 표시용 요소(아이콘 dot, 구분선 등) 추가는 허용. **id는 전부 JS가 참조하므로 절대 변경·삭제 금지.**
- `app.py`, `requirements.txt`, `tests/` 수정 금지.
- 외부 리소스 금지: CDN 폰트(Pretendard 포함), 외부 CSS/JS, 웹폰트 파일 추가 모두 불가. 시스템 폰트 스택만 사용.
- 단일 파일 유지: index.html 외 신규 파일 생성 금지.

## 작업 절차

1. 작업 전 현재 상태 git 커밋.
2. `<style>` 블록 교체 + 필요한 class 조정.
3. 검증(아래) 통과 후 커밋 1개로 마무리.

## 디자인 방향: 토스/당근 스타일 미니멀

핵심 원칙: **그림자 대신 여백, 장식 대신 타이포 위계.** 화면은 조용하고, 누르는 것(버튼)만 명확하게.

### 디자인 토큰 (CSS 변수로 정의)

```css
--bg: #f2f4f6;          /* 페이지 배경: 옅은 그레이 (그라데이션 제거) */
--surface: #ffffff;      /* 카드/입력 표면 */
--fill: #f2f4f6;         /* 입력창 채움색 */
--text: #191f28;         /* 본문 텍스트 */
--text-sub: #4e5968;     /* 보조 텍스트 */
--text-weak: #8b95a1;    /* 힌트/캡션 */
--line: #e5e8eb;         /* 헤어라인 구분선 */
--primary: #3182f6;      /* 포인트 블루 (기존 네이비 대체) */
--primary-weak: #e8f3ff; /* 포인트 연한 배경 */
--danger-weak: #fff3e0;  /* 안전 배너 배경 (옅은 주황) */
--danger-text: #b25c00;  /* 안전 배너 텍스트 */
--radius: 14px;          /* 카드 radius */
--radius-sm: 12px;       /* 버튼/입력 radius */
```

폰트 스택: `-apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Pretendard", "Malgun Gothic", "Segoe UI", sans-serif` (로컬에 있으면 쓰고 없으면 시스템 폰트로 폴백 — 파일 추가는 금지).

### 구체 변경 사항

**전체**
- body 배경: 그라데이션 제거 → `--bg` 단색
- 모든 `box-shadow` 제거. 카드는 흰 표면 + 배경 대비만으로 구분 (테두리도 기본 없음, 필요한 곳만 `--line` 1px)
- radius: 카드 14px, 버튼·입력 12px (기존 18~26px 축소)
- 콘텐츠 폭 `min(100%, 560px)` 유지, 모바일 우선

**히어로**
- 로고 그림자 제거, 로고 크기 36px로 축소, radius 10px
- 브랜드명 1.25rem 수준으로 축소, 아래 설명 문구는 `--text-weak` 0.9rem
- 전체적으로 "앱 상단 타이틀바" 느낌으로 가볍게 (현재의 큰 히어로 영역 축소)

**홈 화면 (프리셋 선택)**
- 그룹 라벨("블로그 글쓰기"/"쇼핑 리뷰"): 0.8rem, `--text-weak`, 자간 살짝, 굵기 600 — 토스의 섹션 캡션 스타일
- 프리셋 버튼: 카드형 → **리스트 행 스타일**. 흰 배경 행에 좌측 프리셋 이름(굵기 600, `--text`), 우측에 `›` 셰브론(`--text-weak`). 행 사이 `--line` 헤어라인. 탭 시 배경 `#f8f9fa` 눌림 효과
- "⚙ 내 말투 설정": 목록 아래 별도 행, 같은 스타일

**입력 폼**
- 라벨: 0.85rem, 굵기 600, `--text-sub`
- 입력창: **테두리 없는 채움형** — 배경 `--fill`, radius 12px, 포커스 시 배경 흰색 + `--primary` 1.5px 테두리 (토스 인풋 스타일)
- 힌트 텍스트: 0.8rem `--text-weak`
- 안전 배너: `--danger-weak` 배경 + `--danger-text` 텍스트, radius 12px, 아이콘 없이 텍스트만 (지금 문구 그대로)
- "← 뒤로": 텍스트 버튼 스타일 (배경 없음, `--text-sub`)

**버튼**
- primary: `--primary` 단색 채움, 흰 텍스트, 굵기 700, radius 12px, 그림자 없음, `:active`에서 밝기 92% (`filter: brightness(.92)`)
- secondary: `--fill` 배경 + `--text` 텍스트 (테두리 없음) — 토스의 회색 보조 버튼
- 높이 넉넉하게 (padding 0.9rem)

**결과 화면**
- 프롬프트 textarea: `--fill` 배경, 고정폭 느낌의 0.9rem, 테두리 없음
- 검수 체크리스트: `--primary-weak` 배경 박스 안에 목록, 텍스트 `--text-sub`
- status 메시지: 초록 배경 대신 텍스트만 (`--primary` 색, 굵기 600)
- "사용 순서" 카드: 배경 없이 헤어라인 위 캡션 목록으로 격하 (존재감 낮춤)

**기타**
- `.section-badge`(무료 사용/복사해서 사용 등 배지): 스타일을 `--primary-weak` 배경 + `--primary` 텍스트로 톤 다운하거나, 촌스럽다고 판단되면 `display:none` 처리 (텍스트 삭제는 금지, 숨김만 허용)
- `<meta name="theme-color">`를 새 배경색 `#f2f4f6`으로 갱신 (이것만 head 수정 허용)
- 다크 모드는 만들지 않음 (라이트 단일)

## 검증 (완료 조건)

1. **기능 무결성**: 헤드리스 브라우저로 쿠팡 프리셋에 황금 표본과 같은 입력을 넣고 "만들 결과" 4종 출력이 `tests/golden/*.txt`와 문자 단위 일치.
2. 4개 프리셋 각각 홈 → 폼 → 생성 → 결과 → 복사 관통 동작, 콘솔 에러 없음.
3. 말투 프로필 저장/불러오기 정상.
4. 안전 배너·체크리스트가 마운자로 프리셋에서 여전히 표시되고 시각적으로 구분됨.
5. 모바일 뷰포트(390px)에서 가로 스크롤 없음, 터치 타겟(버튼·행) 높이 44px 이상.
