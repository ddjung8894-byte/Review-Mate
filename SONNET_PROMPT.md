# Review Mate V1 개편 — 구현 지시서

당신은 이 리포지토리의 `index.html`을 개편하는 구현 담당입니다. 이 문서가 유일한 스펙입니다. 여기 없는 기능은 임의로 추가하지 말고, 판단이 필요하면 이 문서의 원칙(맨 아래 "금지 사항")을 따르세요.

## 프로젝트 배경

Review Mate는 혼자 쓰는 개인용 프롬프트 생성 웹앱입니다. 사용자가 메모를 입력하면, ChatGPT/Claude 웹채팅에 붙여넣을 프롬프트 텍스트를 만들어줍니다. **AI API는 절대 호출하지 않으며**, 출력은 항상 "복사할 프롬프트"입니다.

- 공식 대상: `index.html` **단일 파일** (HTML+CSS+JS 전부 포함. 빌드 도구·프레임워크·npm·외부 CDN 금지)
- `app.py`(Streamlit 구버전)와 `requirements.txt`는 **수정·삭제 금지**
- 로그인·서버·DB·네이버 자동 발행 금지. 저장은 localStorage만
- 모든 UI 텍스트·프롬프트 문구는 한국어

현재 index.html은 쿠팡 리뷰 프롬프트 생성 폼 하나뿐입니다. 이것을 **프리셋 4종(쿠팡 리뷰 / 맛집 후기 / 당뇨·마운자로 기록 / 일상글) + 말투 프로필** 구조로 개편합니다.

## 작업 전 필수 절차

1. `index.html` 전체를 읽으세요.
2. 현재 상태를 git 커밋하세요 (되돌릴 지점).
3. **황금 표본 저장**: 개편 전 코드의 `buildPrompt()` 로직으로 대표 입력 세트 1개 × "만들 결과" 4종의 출력을 텍스트 파일로 저장해두세요 (예: `tests/golden/` 폴더). 개편 후 같은 입력의 출력과 문자 단위로 일치해야 합니다.

## 유지할 것 (수정 금지)

- `<head>`의 PWA 설정 (manifest, 아이콘, theme-color)
- CSS 디자인 시스템 전체 (색 변수, `.mobile-card`, `.field`, 버튼, `.status`, 반응형) — 새 화면도 전부 기존 클래스 재사용
- `safeValue()` 함수
- **쿠팡 프롬프트 문구 전체** (조건 11줄, 재료 라벨, "만들 결과" 4종 분기 문구, 사진 메모 fallback 문구) — 한 글자도 바꾸지 말고 데이터로 이식
- 복사 버튼 로직 (`navigator.clipboard` + 실패 시 `focus()+select()` 폴백)
- "사용 순서" 카드 (결과 화면 하단으로 이동)

## 새 화면 구조

4개 `<section>`을 JS 표시/숨김으로 전환. 화면 상태는 변수 하나(`currentScreen`). URL 해시·라우터 금지.

```
[A] 홈 #screen-home
    "블로그 글쓰기" 그룹: [당뇨·마운자로 기록] [맛집 후기] [일상글]
    "쇼핑 리뷰" 그룹: [쿠팡 리뷰]
    하단: "⚙ 내 말투 설정" 버튼
[B] 입력 폼 #screen-form (프리셋 선택 시 동적 렌더)
    ← 뒤로 | 프리셋 이름 | (안전 프리셋이면 상단 안내 배너) | 필드들 | [프롬프트 만들기]
[C] 결과 #screen-result
    (안전 프리셋이면 검수 체크리스트) | 프롬프트 textarea(readonly)
    [프롬프트 복사하기] [입력 고치기] [처음으로] | 사용 순서 카드
[D] 말투 프로필 #screen-profile
    항목 입력 + [저장] → localStorage 저장 후 홈으로
```

- 히어로(로고+브랜드)는 공통 유지, 문구는 "블로그 글과 리뷰의 프롬프트를 만들어드립니다" 수준으로 확대
- "입력 고치기"는 화면 B로 복귀하며 입력값 유지 (DOM을 지우지 않으면 자동 유지됨)

## 프리셋 데이터 구조

스크립트 상단에 상수로 정의:

```js
const PRESETS = {
  id: {
    name,               // 표시 이름
    group,              // 'blog' | 'review' (홈 그룹핑)
    intro,              // [1] 블록 첫 문장
    fields: [ { key, label, type, placeholder, hint, options, fallback } ],
                        // type: 'text' | 'textarea' | 'select' | 'checkbox'
                        // hint: 필드 아래 상시 안내문 (없으면 null)
    conditions: [ '문자열' 또는 { text, when: (inputs) => bool } ],
    resultModes,        // 쿠팡만: '만들 결과' 선택값 → 추가 요청 문구 매핑
    safetyBlockId,      // 'health' | null
    profileScope,       // 'tone'(말투만) | 'full'(말투+페르소나)
    checklist,          // 결과 화면 체크리스트 배열 | null
    closing             // [5] 블록 오버라이드 | null(공통 문구)
  }
}
const SAFETY_BLOCKS = { health: [ /* 아래 안전 블록 문구 */ ] };
```

## 프리셋 4종 정의

### coupang — 쿠팡 리뷰 (group 'review', profileScope 'tone', safetyBlockId null)
필드·라벨·placeholder·조건 11줄·resultModes 4종을 기존 index.html에서 **그대로** 이식.
빈 필드는 기존대로 `safeValue()` "미입력" 처리 (사진 메모는 기존 fallback 문구).

### matjip — 맛집 후기 (group 'blog', profileScope 'full')
필드:
- 가게 이름·위치 (text)
- 먹은 메뉴와 가격 (textarea)
- 맛·양 솔직 평가 (textarea)
- 분위기·서비스 (textarea)
- 그날의 에피소드 (textarea)
- 재방문 의사 (select: 또 가고 싶음 / 근처 가면 갈 듯 / 한 번이면 충분)
- 당뇨인 시점 코멘트 (textarea, 선택 입력)
- 방문 계기 (select: 내돈내산 / 지인 추천 / 협찬·대가성)
- 사진 자리 표시 (checkbox)

전용 조건:
- 실제 방문 후기처럼 자연스럽게, 가게 홍보 문구처럼 마무리하지 않기
- 단점은 부드럽되 반드시 1개 이상 포함
- 분량 1,000자 내외, 소제목 없이 또는 2~3개
- (방문 계기=협찬·대가성일 때만) 글 서두에 대가성 표기 문구 포함
- (방문 계기≠협찬일 때만) "내돈내산 방문 후기" 관점 유지
- (당뇨 코멘트 입력 시만) 혈당 관리 중인 사람의 개인적 선택 기준으로만 서술, 특정 메뉴가 혈당에 좋다/나쁘다 단정 금지
- (사진 자리 표시 체크 시만) 사진 들어갈 자리에 "(사진: 장면 설명)" 마커 넣기

### mounjaro — 당뇨·마운자로 기록 (group 'blog', safetyBlockId 'health')
필드:
- 몇 주차/기간 (text) — 예: "마운자로 5주차"
- 체중·수치 변화 (text, hint: "기록용입니다. AI에게 해석을 요청하는 용도가 아닙니다.")
- 식욕·식단 변화 (textarea)
- 몸 상태·부작용 걱정 (textarea)
- 이번 주 잘한 것·아쉬운 것 (textarea)
- 솔직한 심경 (textarea)
- 다음 주 목표 (text)

전용 조건:
- "○주차 기록" 연재체, 일기·기록체로 작성 (위 말투 설정과 충돌하면 이 조건을 우선)
- 다음 주 목표를 글의 마무리 훅으로 사용

폼 상단 배너: "이 프리셋은 개인 기록용입니다. 의학적 조언 요청 문구는 프롬프트에 포함되지 않습니다."

checklist (결과 화면에 표시, 체크 상태 저장 안 함):
- 복사 전: 약 권유·용량 조언처럼 읽히는 문장이 없나요?
- 게시 전: 수치가 실제 기록과 일치하나요?
- 게시 전: 면책 문구가 그대로 남아 있나요?
- 게시 전: 내 문장으로 바꾼 곳이 한 군데 이상 있나요?

### daily — 일상글 (group 'blog', profileScope 'full')
필드:
- 오늘 쓰고 싶은 이야기 (큰 textarea)
- 글의 분위기 (select: 담담하게 / 유쾌하게 / 진지하게 / 알아서)
- 꼭 들어갔으면 하는 문장 (text, 선택 입력)
- 마무리 느낌 (select: 여운 남기기 / 다짐으로 / 열린 질문으로 / 알아서)

전용 조건:
- 정보글처럼 정리하지 말기, 소제목·번호 금지
- 교훈으로 억지 마무리하지 않기
- (꼭 들어갔으면 하는 문장 입력 시만) 그 문장을 원문 그대로 자연스러운 위치에 배치

closing 오버라이드: 공통 마무리에서 [확인 필요] 문구를 뺀 버전.

### 신규 프리셋(matjip/mounjaro/daily) 공통 규칙
- **빈 필드는 프롬프트에서 아예 생략** ("미입력" 출력 금지 — 쿠팡만 예외)
- 공통 조건 6줄을 [3] 블록 앞부분에 삽입:
  1. 실제 사람이 쓴 것처럼 자연스럽게, 광고 문구 금지
  2. 입력 재료에 없는 사실·경험 지어내지 않기
  3. 과장 금지, 최상급 표현("인생 최고", "무조건") 남발 금지
  4. 입력된 표현·말투·유머는 어색하지 않게 살리기
  5. 불확실한 부분은 [확인 필요] 표시
  6. (프로필 있을 때만) 위 말투 설정 준수

## 안전 블록 (SAFETY_BLOCKS.health) — 아래 문구 그대로

```
- 이 글은 개인 기록이며 의학 정보 제공이나 조언이 아님
- 약을 권유하거나 추천하는 문장 생성 금지
- 용량·투여 방법에 대한 조언이나 일반화 금지 (사실 기록은 가능)
- 치료 효과 단정 금지 ("효과가 있다" 대신 "내 경우에는 이랬다"로)
- 입력된 수치는 그대로 인용만 하고, 해석·평가 문장 생성 금지
- 모든 변화는 "내 경우에는 / 나는 이렇게 느꼈다" 프레임으로 서술
- 글 맨 마지막에 다음 문구를 그대로 포함: "이 글은 개인 기록이며 의학적 조언이 아닙니다. 약과 건강에 관한 결정은 반드시 주치의와 상의하세요."
```

이 블록은 `safetyBlockId`가 있으면 조립기가 **무조건** 삽입합니다. 이를 끄거나 우회하는 옵션·스위치를 어디에도 만들지 마세요.

## 프롬프트 조립기

```
assemblePrompt(preset, inputs, profile):
  [1] preset.intro
  [2] 프로필 블록 — profile이 null이면 블록 전체 생략.
      profileScope가 'tone'이면 페르소나(소개 한 줄) 제외.
      예: "글쓴이 정보: 40대, 당뇨 관리 중인 직장인. 존댓말, 이모지는 가끔,
          담백한 문체. 항상 지킬 것: 특정 브랜드 언급 자제"
  [3] "조건:" 헤더 + 공통 조건 6줄(신규 프리셋만) + preset.conditions(when 통과분)
      + resultModes 추가 요청(쿠팡만) + SAFETY_BLOCKS[safetyBlockId](있으면 항상 마지막)
  [4] 재료 — fields 순회하며 "라벨:\n값".
      쿠팡: 빈 값 → safeValue() fallback. 신규 프리셋: 빈 값 → 항목 자체 생략
  [5] preset.closing 또는 공통 마무리:
      "이건 초안이야. 내가 직접 고쳐서 올릴 거니까 완성된 척하지 말고,
       확실하지 않은 정보는 [확인 필요]라고 표시해줘."
  블록들을 빈 줄로 join해 반환
```

**핵심 제약**: 쿠팡 프리셋은 프로필 미설정 시 [2]와 [5]를 모두 생략하고 공통 조건 6줄도 넣지 않아, **기존 index.html의 출력과 문자 단위로 동일**해야 합니다. 황금 표본과 diff가 없어야 1단계 통과입니다.

## 말투 프로필 (화면 D)

항목: 존댓말/반말/섞어서 · 이모지(안 씀/가끔/자주) · 문장 성격(담백하게/수다스럽게/유머 섞어서) · 내 소개 한 줄(자유 입력, 빈 값 허용) · 항상 지킬 것(자유 입력, 빈 값 허용)

```
localStorage 키 rm_profile:
{ "tone": "polite"|"casual"|"mixed", "emoji": "none"|"some"|"often",
  "style": "plain"|"chatty"|"humor", "persona": "", "alwaysRules": "",
  "updatedAt": ISO 문자열 }
localStorage 키 rm_schema_version: "1"
```

- `loadProfile()`/`saveProfile()` 유틸 2개로 감싸기. JSON 파싱 실패 시 null 반환(프로필 없음 취급)
- localStorage 접근 불가(시크릿 모드 등)는 try/catch로 "프로필 저장이 안 되는 브라우저입니다" 안내만 표시하고 앱은 계속 동작
- `rm_history`(입력 이력)는 **구현하지 않습니다** (V1.1 예정, 키 이름만 예약)

## 복사 버튼

기존 로직 이식 + 개선 2가지:
- 복사 성공 시 버튼 라벨을 2초간 "복사 완료 ✓"로 변경 후 원복
- 폴백 순서: `navigator.clipboard` 실패 → `document.execCommand('copy')` 시도 → 그래도 실패하면 `focus()+select()` 후 수동 복사 안내

## 작업 순서 (단계마다 테스트 통과 후 git 커밋)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 0 | 현재 상태 커밋 + 황금 표본 저장 | 되돌릴 지점 확보 |
| 1 | 4화면 골격 + PRESETS/SAFETY_BLOCKS 구조 + 조립기 + 쿠팡 이식 + 복사 버튼 이식 | 황금 표본 4종과 문자 단위 일치 |
| 2 | 말투 프로필 화면 + localStorage + [2]/[5] 블록 주입 | 저장 후 새로고침해도 유지. 프로필 없을 때 쿠팡 출력 불변 |
| 3 | mounjaro 프리셋 + 안전 블록 + 배너 + 체크리스트 | 안전 문구 7줄(면책 포함)이 항상 포함. tone을 반말로 바꿔도 일기체 조건 유지 |
| 4 | matjip 프리셋 | 협찬/내돈내산·당뇨 코멘트·사진 마커 분기가 입력대로 켜지고 꺼짐 |
| 5 | daily 프리셋 + 히어로 문구·README.md 갱신 | 4개 프리셋 관통(홈→입력→생성→복사) 정상, 콘솔 에러 없음 |

각 단계는 독립적으로 배포 가능한 완결 상태로 끝내세요. 매 단계 공통 확인: 홈↔폼↔결과 왕복 시 입력값 유지, 콘솔 에러 없음.

## 금지 사항 요약

- `app.py`·`requirements.txt` 수정·삭제 금지
- index.html 외 신규 JS/CSS 파일 생성 금지 (테스트용 황금 표본 텍스트 파일은 예외)
- AI API 호출 코드, 외부 CDN·라이브러리, 로그인·서버·DB 금지
- 안전 블록 우회 스위치 금지
- rm_history(입력 이력) 구현 금지 — 이번 범위 아님
- 쿠팡 프롬프트 문구 변경 금지
- 이 문서에 없는 기능 추가 금지 (개인용 도구입니다 — 단순하게 유지)
