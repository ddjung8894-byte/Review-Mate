index.html 개편 구현 계획
1. 유지할 부분
대상	위치	판단
PWA 헤드 (manifest, 아이콘, theme-color)	1~12행	그대로 유지
CSS 디자인 시스템 전체 (색 변수, .mobile-card, .field, 입력 스타일, 버튼, .status, 반응형)	13~218행	그대로 유지. 새 화면도 전부 이 클래스 재사용
히어로 (로고 + 브랜드)	222~228행	구조 유지, 문구만 수정 (아래 2번)
safeValue()	327~330행	그대로 재사용
쿠팡 프롬프트 원문 텍스트 (조건 11줄 + 재료 라벨)	348~383행	문구 한 글자도 바꾸지 않고 프리셋 데이터로 이식
"만들 결과" 4종 분기 (buildExtraRequest)	332~343행	로직 그대로, 프리셋 옵션으로 이식
복사 버튼 로직 (clipboard + 폴백 + 상태 문구)	404~419행	그대로 재사용
"사용 순서" 카드, 하단 mini-note	296~310행	유지, 결과 화면 하단으로 이동
2. 바꿀 부분
본문 구조: 현재 "폼 카드 + 결과 카드"가 항상 동시에 보이는 1페이지 → 4개 <section> 화면을 표시/숨김으로 전환하는 구조로 교체 (222~311행 재배치)
히어로 문구: "리뷰 프롬프트를 만들어드립니다" → "블로그 글과 리뷰의 프롬프트를 만들어드립니다" 수준으로 확대
하드코딩 폼: 쿠팡 전용 HTML 폼(230~283행) 삭제 → 프리셋 정의를 읽어 폼을 그리는 렌더 함수로 대체
buildPrompt(): 쿠팡 전용 템플릿 함수(345~384행) → 5블록 공통 조립기로 대체
fields 객체: 고정 id 참조(314~325행) → 렌더 시점에 프리셋 필드 키로 수집하는 방식으로 대체
신규 추가: localStorage 읽기/쓰기 유틸, 말투 프로필 화면, 안전 블록 상수, 검수 체크리스트 표시
app.py 처리 (제안만): 삭제하지 않고 legacy/app.py로 이동하는 것을 제안합니다. git mv app.py legacy/app.py 한 번이면 이력이 보존되고, 루트에는 공식 대상인 index.html만 남아 혼동이 사라집니다. 이동 여부는 "진행" 지시에 포함해서 알려주시면 되고, 지시가 없으면 app.py는 건드리지 않습니다.

3. 새 화면 구조
단일 index.html 안에서 4개 섹션을 JS로 전환합니다. 화면 상태는 변수 하나(currentScreen)로만 관리하고 라우터·해시 처리는 하지 않습니다.


<main>
 ├ 히어로 (모든 화면 공통, 홈에서만 크게)
 │
 ├ [화면 A] 홈 #screen-home
 │   "블로그 글쓰기" 그룹
 │     [당뇨·마운자로 기록] [맛집 후기] [일상글]
 │   "쇼핑 리뷰" 그룹
 │     [쿠팡 리뷰]
 │   ⚙ 내 말투 설정 (하단 텍스트 버튼)
 │
 ├ [화면 B] 입력 폼 #screen-form  (프리셋 선택 시 동적 렌더)
 │   ← 뒤로 | 프리셋 이름
 │   (안전 프리셋이면) 상단 안내 배너
 │   필드들 (프리셋 정의 순서대로)
 │   ▸ 세부 옵션 (접힘: 말투·분량 이번만 바꾸기)
 │   [프롬프트 만들기]
 │
 ├ [화면 C] 결과 #screen-result
 │   (안전 프리셋이면) 검수 체크리스트
 │   완성 프롬프트 textarea (readonly)
 │   [프롬프트 복사하기]  ← 기존 로직 이식
 │   [입력 고치기]  [처음으로]
 │   "사용 순서" 카드 (기존 것 이동)
 │
 └ [화면 D] 말투 프로필 #screen-profile
     존댓말/반말 · 이모지 · 문장 성격 · 소개 한 줄 · 항상 지킬 것
     [저장] → localStorage, 저장 후 홈으로
"입력 고치기"는 화면 B로 돌아가되 입력값을 지우지 않습니다(같은 DOM이므로 자동으로 유지됨 — 별도 상태 저장 불필요).

4. 프리셋 데이터 객체 구조
스크립트 상단에 상수로 정의합니다. 이 구조가 이번 개편의 중심입니다.


PRESETS = {
  coupang: {
    name: '쿠팡 리뷰',
    group: 'review',                    // 홈 화면 그룹핑용
    intro: '아래 메모를 바탕으로 쿠팡 리뷰처럼 자연스럽게 써줘.',  // [1] 블록
    fields: [ ...필드 정의(5번) ],
    conditions: [ ...조건 줄 배열 ],     // [3] 블록 (기존 11줄 그대로)
    resultModes: { ...만들 결과 4종 → 추가 요청 문구 },
    safetyBlockId: null,                // 안전 블록 참조 (없으면 null)
    profileScope: 'tone',               // 'tone' = 말투만 | 'full' = 말투+페르소나
    checklist: null,                    // 결과 화면 체크리스트 (안전 프리셋만)
    closing: null                       // [5] 블록 오버라이드 (기본은 공통 문구)
  },
  matjip:   { ... group: 'blog', profileScope: 'full' },
  mounjaro: { ... group: 'blog', safetyBlockId: 'health', checklist: [...], 톤 고정 },
  daily:    { ... group: 'blog', 조건 최소 }
}
필드 1개의 정의 형태:


{ key: 'menuPrice', label: '먹은 메뉴와 가격', type: 'textarea',
  placeholder: '예: 제육볶음 9,000원 ...', fallback: '미입력',
  hint: null }        // hint: 필드 아래 상시 안내문 (수치 입력란 등에 사용)
// type: 'text' | 'textarea' | 'select'(options 배열) | 'checkbox'
설계 결정 두 가지:

쿠팡의 resultModes는 쿠팡 프리셋에만 존재하는 선택 필드로 둡니다. 다른 프리셋에 억지로 일반화하지 않습니다 (일상글에 "제목 5개만"은 필요 없음).
조건부 문구(맛집의 협찬 분기, 당뇨 코멘트 유무 분기)는 조건 줄에 when: (inputs) => ... 형태의 함수 한 칸을 허용해서 처리합니다. 프리셋마다 전용 조립 함수를 만드는 것을 막기 위한 최소한의 장치입니다.
5. 프리셋별 입력 필드 정의
쿠팡 리뷰 (기존과 1:1 동일, 라벨·placeholder까지 그대로)
만들 결과(select 4종) / 상품명 / 왜 구매했나요? / 좋았던 점 / 아쉬웠던 점 / 실제 사용하면서 있었던 일 / 추천할 사람 / 사진을 보고 적어둘 점 (fallback: "사진 분석은 하지 않고…" 기존 문구 유지)

맛집 후기
가게 이름·위치(text) / 먹은 메뉴와 가격(textarea) / 맛·양 솔직 평가(textarea) / 분위기·서비스(textarea) / 그날의 에피소드(textarea) / 재방문 의사(select: 또 가고 싶음·근처 가면 갈 듯·한 번이면 충분) / 당뇨인 시점 코멘트(textarea, 선택) / 방문 계기(select: 내돈내산·지인 추천·협찬/대가성) / 사진 자리 표시(checkbox)

당뇨·마운자로 기록
몇 주차/기간(text) / 체중·수치 변화(text, hint: "기록용입니다. AI에게 해석을 요청하는 용도가 아닙니다") / 식욕·식단 변화(textarea) / 몸 상태·부작용 걱정(textarea) / 이번 주 잘한 것·아쉬운 것(textarea) / 솔직한 심경(textarea) / 다음 주 목표(text)

일상글
오늘 쓰고 싶은 이야기(큰 textarea) / 글의 분위기(select: 담담하게·유쾌하게·진지하게·알아서) / 꼭 들어갔으면 하는 문장(text, 선택) / 마무리 느낌(select: 여운·다짐·열린 질문·알아서)

빈 값 처리: 쿠팡은 기존대로 "미입력" 출력(호환성 유지), 신규 3종은 빈 필드를 프롬프트에서 아예 생략 — "미입력"이 줄줄이 붙은 프롬프트보다 깔끔하고, AI가 빈 항목을 지어내는 것도 방지됩니다.

6. 공통 프롬프트 조립 함수 구조

assemblePrompt(preset, inputs, profile)
  → [1] preset.intro
  → [2] renderProfileBlock(profile, preset.profileScope)
         · 프로필 없으면 블록 생략 (프리셋 기본 톤 조건만으로 동작)
         · scope가 'tone'이면 소개 한 줄(페르소나) 제외
  → [3] 조건 블록
         · preset.conditions 중 when 조건 통과한 줄
         · + resultModes 추가 요청 (쿠팡만)
         · + SAFETY_BLOCKS[preset.safetyBlockId] (있으면, 항상 마지막에)
  → [4] 재료 블록: fields 순회 → "라벨:\n값" (빈 값은 프리셋 정책대로 생략 or fallback)
  → [5] preset.closing || 공통 마무리
         "이건 초안이야. 내가 직접 고쳐서 올릴 거니까 ..."
         · 일상글은 [확인 필요] 문구를 뺀 전용 closing 사용
  → 블록들을 빈 줄로 join해서 반환
쿠팡 프리셋에 한해 [2]와 [5] 블록도 프로필 미설정 시 완전히 생략되도록 해서, 프로필을 안 만든 상태의 쿠팡 출력이 기존 출력과 동일하게 유지되게 합니다 (12번 안전 조치의 근거).

7. 말투 프로필 localStorage 구조

키: rm_profile
{
  "tone": "polite" | "casual" | "mixed",
  "emoji": "none" | "some" | "often",
  "style": "plain" | "chatty" | "humor",
  "persona": "40대, 당뇨 관리 중인 직장인",   // 빈 문자열 허용
  "alwaysRules": "특정 브랜드 언급 자제",      // 빈 문자열 허용
  "updatedAt": "2026-07-07T…"
}

키: rm_schema_version → "1"
읽기/쓰기는 loadProfile() / saveProfile() 유틸 2개로 감싸고, JSON 파싱 실패 시 null 반환(프로필 없음과 동일 취급) — localStorage가 깨져도 앱은 동작.
rm_history(입력 이력)는 이번 범위에서 구현하지 않음. 키 이름만 예약.
시크릿 모드 등 localStorage 접근 자체가 막힌 환경은 try/catch로 감싸 "프로필 저장이 안 되는 브라우저입니다" 안내만 표시.
8. 복사 버튼 구현 방식
기존 로직(404~419행)을 그대로 결과 화면으로 이식합니다: navigator.clipboard.writeText 시도 → 실패 시 focus()+select() 후 수동 복사 안내. 추가 개선 2가지만:

복사 성공 시 버튼 라벨을 2초간 "복사 완료 ✓"로 변경 (모바일에서 status 문구보다 눈에 잘 띔)
폴백에 document.execCommand('copy') 한 줄 추가 시도 (구형 iOS Safari 대응, 실패해도 기존 수동 안내로 자연스럽게 이어짐)
9. 당뇨·마운자로 안전 블록 구현 방식
상수 분리: 프리셋 정의와 별도로 SAFETY_BLOCKS = { health: [...조건 줄 배열] } 상수로 정의하고 프리셋은 id로만 참조. V1.1의 건강 정보 정리글이 같은 블록을 재사용할 수 있게 하기 위함입니다.

블록 내용 (지시하신 6개 항목 전부 포함):


- 이 글은 개인 기록이며 의학 정보 제공이나 조언이 아님
- 약을 권유하거나 추천하는 문장 생성 금지
- 용량·투여 방법에 대한 조언이나 일반화 금지 (사실 기록은 가능)
- 치료 효과 단정 금지 ("효과가 있다" → "내 경우에는 이랬다")
- 입력된 수치는 그대로 인용만, 해석·평가 문장 생성 금지
- 모든 변화는 "내 경우에는 / 나는 이렇게 느꼈다" 프레임으로 서술
- 글 맨 마지막에 다음 문구를 그대로 포함:
  "이 글은 개인 기록이며 의학적 조언이 아닙니다.
   약과 건강에 관한 결정은 반드시 주치의와 상의하세요."
끌 수 없게 하는 방법: 이 블록은 조립기에서 safetyBlockId가 있으면 무조건 삽입되며, 세부 옵션·프로필 어디에도 이를 우회하는 스위치를 만들지 않습니다. 또한 마운자로 프리셋은 프로필의 tone 값과 무관하게 일기체 고정 조건을 프리셋 conditions에 넣습니다(프로필 블록보다 [3]이 뒤에 오므로 우선 적용됨을 조건 문구로 명시: "위 말투 설정과 충돌하면 이 조건을 우선").

UI 측 장치:

폼 상단 배너: "이 프리셋은 개인 기록용입니다. 의학적 조언 요청 문구는 프롬프트에 포함되지 않습니다."
결과 화면 체크리스트 (preset.checklist에서 렌더):

복사 전: □ 약 권유·용량 조언처럼 읽히는 문장이 없나요?
게시 전: □ 수치가 실제 기록과 일치하나요?
        □ 면책 문구가 그대로 남아 있나요?
        □ 내 문장으로 바꾼 곳이 한 군데 이상 있나요?
체크 상태는 저장하지 않는 단순 표시용(강제 아님).
입력 단어 감지(노란 안내)는 이번 범위에서 제외 — 오탐 조정에 품이 들고, 1겹(프롬프트)과 3겹(체크리스트)만으로 V1은 충분합니다. V1.1 후보로 미룹니다.
10. 작업 순서
지시하신 우선순위(쿠팡 이식 → 복사 → 프로필 → 안전 프리셋)를 반영했습니다.

단계	내용	완료 기준
0	git 커밋으로 현재 상태 저장 (+ 승인 시 app.py → legacy/ 이동)	되돌릴 지점 확보
1	4화면 골격 + 프리셋 데이터 구조 + 조립기 + 쿠팡 이식 + 복사 버튼 이식	새 구조에서 쿠팡 프롬프트가 기존과 동일하게 나옴
2	말투 프로필 화면 + localStorage + [2]/[5] 블록 주입	프로필 저장 후 재접속해도 유지, 프롬프트에 반영
3	당뇨·마운자로 프리셋 + 안전 블록 + 배너 + 체크리스트	안전 문구 6종이 프롬프트에 빠짐없이 포함
4	맛집 후기 프리셋 (협찬 분기, 당뇨 코멘트 분기 포함)	분기 문구가 입력에 따라 정확히 켜지고 꺼짐
5	일상글 프리셋 + 히어로 문구·README 갱신	전 프리셋 관통 테스트 통과
각 단계는 독립적으로 배포 가능한 상태로 끝납니다 — 어느 단계에서 멈춰도 앱은 완전한 상태.

11. 단계별 테스트 방법
빌드 도구가 없으므로 테스트는 전부 "브라우저에서 열고 확인"입니다. 단계마다 아래를 수행합니다.

1단계 (핵심 테스트): 개편 전에 현재 버전에서 대표 입력 세트 1개로 프롬프트를 생성해 텍스트 파일로 저장해 둠 → 개편 후 같은 입력으로 생성한 결과와 문자 단위 비교 ("만들 결과" 4종 각각). 일치해야 통과.
2단계: 프로필 저장 → 브라우저 새로고침 → 프로필 화면에 값 유지 확인. 프로필 있는 상태/없는 상태(localStorage 삭제 후) 각각 쿠팡·프롬프트 생성 확인. 시크릿 창에서 열어 저장 불가 안내 확인.
3단계: 마운자로 프리셋에서 (a) 전 필드 입력, (b) 주차만 입력 두 케이스 생성 → 안전 문구 6종 + 면책 문구가 둘 다 포함되는지 눈으로 대조. 프로필 tone을 '반말'로 바꿔도 일기체 고정 조건이 프롬프트에 남는지 확인.
4단계: 방문 계기를 협찬/내돈내산으로 바꿔가며 표기 문구 on/off 확인. 당뇨 코멘트 빈 값일 때 관련 조건이 프롬프트에서 사라지는지 확인.
5단계 (관통 테스트): 폰 브라우저(실기기)에서 4개 프리셋 각각 "홈 → 입력 → 생성 → 복사 → ChatGPT 붙여넣기"를 실제 수행. 복사 폴백 확인을 위해 iOS Safari 1회 포함 권장.
매 단계 공통: 홈↔폼↔결과 왕복 시 입력값 유지 확인, 브라우저 콘솔에 에러 없음 확인.
12. 쿠팡 리뷰 기능을 지키는 안전 조치
git 스냅샷: 작업 시작 전 현재 index.html을 커밋. 문제가 생기면 git checkout으로 즉시 복구 — 파일이 하나라 이것이 가장 확실한 보험입니다.
황금 표본 비교: 11번 1단계의 "개편 전 프롬프트 저장 → 개편 후 문자 단위 비교". 쿠팡 프롬프트 문구는 데이터로 옮길 뿐 한 글자도 수정하지 않는 것이 원칙이므로, diff가 없어야 정상.
프로필 미설정 = 기존과 동일 출력: 6번에서 설명한 대로, 쿠팡 프리셋은 프로필이 없으면 [2]/[5] 블록이 생략되어 기존 출력과 완전 동일. 2단계(프로필) 작업이 쿠팡 출력을 바꾸는 사고를 구조적으로 차단.
쿠팡부터 이식: 신규 프리셋을 만들기 전에 기존 기능을 새 구조에서 먼저 검증하므로, 조립기 버그는 신규 기능이 아니라 1단계에서 발견됨.
단계별 커밋: 각 단계 완료·테스트 통과 시점마다 커밋. 후속 단계에서 문제가 생겨도 직전 정상 상태로 한 단계만 되돌리면 됨.
계획은 여기까지입니다. 확인하시고 **"진행"**이라고 말씀해 주시면 0단계(커밋)부터 시작하겠습니다. 그때 app.py를 legacy/로 이동할지 여부도 함께 알려주세요 (지시가 없으면 건드리지 않고 진행합니다).